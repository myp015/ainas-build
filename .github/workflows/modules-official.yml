#
# Copyright (C) 2022 Ing <https://github.com/wjz304>
#
# This is free software, licensed under the MIT License.
# See /LICENSE for more information.
#

name: Get modules official

on:
  workflow_dispatch:

jobs:
  official:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@main
        with:
          repository: myp015/ainas-modules
          token: ${{ secrets.AiNas}}
          path: ainas-modules

      - name: Checkout
        uses: actions/checkout@main
        with:
          repository: RROrg/syno-modules
          token: ${{ secrets.AiNas }}
          path: syno-modules

      - name: Init Env
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          sudo timedatectl set-timezone "Asia/Shanghai"

      - name: Get official modules
        run: |
          ROOT_PATH=${{ github.workspace }}/syno-modules ${{ github.workspace }}/ainas-modules/getOfficial.sh "apollolake broadwell broadwellnk broadwellnkv2 broadwellntbap denverton epyc7002 epyc7003 epyc7003ntb geminilake geminilakenk icelaked purley r1000 r1000nk v1000 v1000nk"

      - name: Check and Push
        run: |
          cd syno-modules
          git pull
          status=$(git status -s | grep -E '(rd|hda1)/' | awk '{printf " %s", $2}')
          if [ -n "${status}" ]; then
            git add ${status}
            git commit -m "update $(date +%Y-%m-%d" "%H:%M:%S)"
            git push -f
          fi
